.venv/

secrets/

# database â€” never commit or deploy the local SQLite file
db/fantasy.db
*.db

# Python bytecode
__pycache__/
*.pyc
*.pyo

# credential rotation output â€” contains a live DB password, never commit
rotate_db_password_output.txt

# Claude Code local permission settings — machine-specific, may contain credentials
.claude/settings.local.json

# DB snapshots — not source, already synced via OneDrive
backups/
